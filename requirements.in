# Runtime dependencies for the FastAPI backend + Postgres/pgvector data layer.
# Compile with: .venv/Scripts/python -m piptools compile requirements.in -o requirements.txt
fastapi>=0.115,<1.0
uvicorn[standard]>=0.32,<1.0
pydantic>=2.9,<3.0
pydantic-settings>=2.6,<3.0
sqlalchemy>=2.0,<3.0
alembic>=1.14,<2.0
psycopg[binary]>=3.2,<4.0
pgvector>=0.3,<1.0
python-dotenv>=1.0,<2.0

# RAG pipeline (Sections 8-10): PDF extraction, chunking, local embeddings.
pymupdf>=1.24,<2.0
pdfplumber>=0.11,<1.0
langchain-text-splitters>=0.3,<1.0
sentence-transformers>=3.0,<4.0

# Agent orchestration (Section 15): LangGraph for the graph itself; two interchangeable LLM
# client libraries behind agents/llm.py's provider switch (Section 17 vendor-lock-in
# mitigation) - Claude (ChatAnthropic) or any OpenAI-compatible server such as a Colab-hosted
# Ollama instance (ChatOpenAI). Only Intent Classification and the Final Answer node's
# narrative touch an LLM at all (Section 13.1/1.2).
langgraph>=0.2,<1.0
langchain-anthropic>=0.3,<1.0
langchain-openai>=0.2,<1.0
